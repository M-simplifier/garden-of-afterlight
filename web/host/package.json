{
  "name": "afterlight-web-host",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/host.js --bundle --format=esm --platform=browser --external:./raylib.mjs --outfile=public/host.js",
    "check": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@bjorn3/browser_wasi_shim": "0.4.2"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
